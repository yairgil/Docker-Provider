exporters:
  file:
    path: /opt/promotel.json
  influx:
    loglevel: debug
receivers:
    prometheus:
      include_resource_labels: true
      config:
        $AZMON_PROMETHEUS_CONFIG
service:
  pipelines:
    metrics:
      receivers: [prometheus]
      exporters: [influx]